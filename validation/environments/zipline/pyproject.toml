[project]
name = "ml4t-backtest-validation-zipline"
version = "0.0.0"
requires-python = ">=3.12,<3.13"
dependencies = ["ml4t-backtest", "zipline-reloaded==3.1.1"]

[tool.uv]
package = false

[tool.uv.sources]
ml4t-backtest = { path = "../../..", editable = true }
